from django.apps import AppConfig

class SaudePublicaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'saudepublica'  # Certifique-se de que o nome da aplicação está correto

